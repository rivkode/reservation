server:
  port: 0

# 단위 · 슬라이스 테스트 기본값. @SpringBootTest(webEnvironment = ...) 에 따라 실제
# 바인딩 유무가 결정된다. Testcontainers 기반 통합 테스트는 MysqlContainerExtension
# 이 spring.datasource.* 를 시스템 프로퍼티로 주입하므로 여기에는 DB 설정을 두지 않는다.
