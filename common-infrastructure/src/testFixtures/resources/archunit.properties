# ArchUnit 1.x 기본값은 `archRule.failOnEmptyShould = true` 이다. 본 프로젝트는
# Phase 0 단계에서 도메인/애플리케이션/인프라/프레젠테이션 패키지가 아직 비어 있고,
# Phase 1 이후에 점진적으로 채워진다. 빈 선언을 매번 실패시키면 스캐폴딩 중 CI 가
# 지속 빨개지므로 `false` 로 두되, 실제 위반(원칙에 어긋나는 import/애노테이션 위치
# 등) 은 여전히 실패로 표면화된다.
archRule.failOnEmptyShould = false
